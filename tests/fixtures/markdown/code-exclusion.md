# Code is not prose

A real link [[Yes]] and a real tag #RealTag.

Inline code holds `[[NotALink]]` and `#nottag` literally.

```sh
#!/bin/sh
# This is a comment, not a heading
echo "[[NotALink]] #nottag"
```

    An indented block with [[NotALink]] and #nottag

Back to prose: [[AlsoYes]] and #AlsoReal.
