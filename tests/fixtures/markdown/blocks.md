# Blocks

A paragraph worth referring to. ^trailing-marker

Another paragraph
that spans two lines.
^standalone-marker

Not a marker: the expression a^b is maths.

Not a marker either: ^has spaces, and ^has/slash.

```
code with ^infence
```
